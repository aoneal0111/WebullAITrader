# Incident response

Activate emergency stop first. Preserve databases and logs. For `UNRESOLVED`, query broker orders and history by client ID; never resubmit automatically. Broker-only orders, position/cash mismatches, stale reconciliation, market outages, credential failures, and database failures keep readiness false. Escalate and reconcile manually before clearing the stop.
